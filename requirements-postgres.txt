# Дополнительные зависимости для подключения к PostgreSQL.
# Нужны только если запускаете через docker-compose или указываете POSTGRES_* в .env.
# Для локального запуска на SQLite (по умолчанию) ставить НЕ нужно.
-r requirements.txt
psycopg2-binary>=2.9.10
